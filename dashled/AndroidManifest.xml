<?xml version="1.0" encoding="utf-8"?>
<!--
     Copyright (C) 2026 GitHub @YorokobiMaster

     SPDX-License-Identifier: Apache-2.0
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.sandai.dashled">

    <!-- Declaration gate only; the real authorization is the App access grant
         checked by calling UID inside DashLedService. -->
    <permission
        android:name="me.sandai.dashled.permission.CONTROL"
        android:protectionLevel="normal" />

    <!-- The App access page scans installed packages for dashled client
         metadata; without this the package-visibility filter hides them. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <application
        android:allowBackup="false"
        android:defaultToDeviceProtectedStorage="true"
        android:directBootAware="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SubSettingsBase.Expressive">

        <activity
            android:name=".LedSettingsActivity"
            android:label="@string/app_name"
            android:exported="true">
            <intent-filter>
                <action android:name="com.android.settings.action.IA_SETTINGS" />
            </intent-filter>

            <meta-data
                android:name="com.android.settings.category"
                android:value="com.android.settings.category.ia.notifications" />
            <meta-data
                android:name="com.android.settings.group_key"
                android:value="general_notifications_category" />
            <meta-data
                android:name="com.android.settings.summary"
                android:resource="@string/master_summary" />
            <meta-data
                android:name="com.android.settings.order"
                android:value="5" />
            <meta-data
                android:name="com.android.settings.profile"
                android:value="primary_profile_only" />
        </activity>

        <activity
            android:name=".NotificationAppsActivity"
            android:label="@string/notif_apps_title"
            android:exported="false" />

        <service
            android:name=".DashLedService"
            android:exported="true"
            android:permission="me.sandai.dashled.permission.CONTROL">
            <intent-filter>
                <action android:name="me.sandai.dashled.BIND" />
            </intent-filter>
        </service>

        <service
            android:name=".LedNotificationListener"
            android:label="@string/app_name"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>
</manifest>
