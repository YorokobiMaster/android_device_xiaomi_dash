<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- VoLTE available -->
    <bool name="config_device_volte_available">true</bool>

    <!-- 5G modes supported by the retained MediaTek radio stack -->
    <bool name="config_telephony5gStandalone">true</bool>
    <bool name="config_telephony5gNonStandalone">true</bool>

    <!-- SIM hot swap -->
    <bool name="config_hotswapCapable">true</bool>

    <!-- Display cutout / camera hole. Copied verbatim from HyperOS
         DevicesAndroidOverlay.apk: a 66px-wide rectangle spanning from
         the top edge down to 152px, matching status_bar_height. -->
    <string name="config_mainBuiltInDisplayCutout" translatable="false">
        M 0,0 H -33 V 152 H 33 V 0 H 0 Z
    </string>

    <!-- Display shape -->
    <string name="config_mainDisplayShape" translatable="false">M20 0h1040s20 0 20 20v2360s0 20 -20 20h-1040s-20 0 -20 -20v-2360s0 -20 20 -20</string>

    <!-- UDFPS Sensor Properties: [sensorLocationX, sensorLocationY, sensorRadius] -->
    <integer-array name="config_udfps_sensor_props" translatable="false">
        <item>640</item>
        <item>2124</item>
        <item>79</item>
    </integer-array>

    <!-- Always-on display. The panel driver provides doze/AOD support
         (doze_enable/doze_disable, AOD brightness tables); present it
         through SystemUI's doze service. -->
    <bool name="config_dozeAlwaysOnDisplayAvailable">true</bool>
    <string name="config_dozeComponent" translatable="false">com.android.systemui/com.android.systemui.doze.DozeService</string>

    <!-- Whether device supports double tap to wake -->
    <bool name="config_supportDoubleTapWake">true</bool>

    <!-- Pickup gesture: advertise support so the upstream Settings switch
         ("Lift to check phone") appears. There is no standard
         TYPE_PICK_UP_GESTURE sensor on this device, so DozeSensors'
         pickup TriggerSensor stays inert; the vendor's pickup Wakeup
         sensor (type 33171036) is fused into the Fod Wakeup event stream
         and DashFod applies the DOZE_PICK_UP_GESTURE setting itself when
         deciding whether a doze-time fod trigger is a raise or a press. -->
    <bool name="config_dozePulsePickup">true</bool>

    <!-- Screen-off UDFPS: advertise support so the upstream Settings switch
         appears. The switch writes secure screen_off_udfps_enabled, which
         DashFod reads. The Doze UDFPS long-press sensor type is deliberately
         NOT declared: xiaomi.sensor.fod is a SPECIAL_TRIGGER sensor, and
         AOSP's requestTriggerSensor only accepts ONE_SHOT sensors, so the
         upstream DozeSensors path can never subscribe to it. Screen-off
         initiation is device-owned: DashFod subscribes via registerListener
         and wakes the display, after which the existing keyguard UDFPS flow
         completes the unlock. -->
    <bool name="config_screen_off_udfps_enabled">true</bool>
</resources>
